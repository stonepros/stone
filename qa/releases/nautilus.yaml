tasks:
- exec:
    osd.0:
      - stone osd require-osd-release nautilus
      - stone osd set-require-min-compat-client nautilus
      - for p in `stone osd pool ls`; do stone osd pool set $p pg_autoscale_mode off; done
- stone.healthy:
