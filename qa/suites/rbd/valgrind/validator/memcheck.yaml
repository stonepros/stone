# see http://tracker.stone.com/issues/20360 and http://tracker.stone.com/issues/18126
os_type: centos

overrides:
  install:
    stone:
      debuginfo: true
  rbd_fsx:
    valgrind: ["--tool=memcheck"]
  workunit:
    env:
      VALGRIND: "--tool=memcheck --leak-check=full"
