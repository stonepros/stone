roles:
- - mon.a
  - mgr.x
  - osd.0
  - osd.1
  - osd.2
- - osd.3
  - osd.4
  - osd.5
  - client.0
openstack:
  - volumes: # attached to each instance
      count: 3
      size: 30 # GB
tasks:
- install:
- stone:
    pre-mgr-commands:
      - sudo stone config set mgr mgr/devicehealth/enable_monitoring false --force
    log-ignorelist:
      - but it is still running
      - slow request
      - overall HEALTH_
      - \(CACHE_POOL_
- exec:
    client.0:
      - sudo stone osd pool create base 4
      - sudo stone osd pool application enable base rados
      - sudo stone osd pool create cache 4
      - sudo stone osd tier add base cache
      - sudo stone osd tier cache-mode cache writeback
      - sudo stone osd tier set-overlay base cache
      - sudo stone osd pool set cache hit_set_type bloom
      - sudo stone osd pool set cache hit_set_count 8
      - sudo stone osd pool set cache hit_set_period 60
      - sudo stone osd pool set cache target_max_objects 500
- background_exec:
    mon.a:
      - while true
      - do sleep 30
      - sudo stone osd pool set cache cache_target_full_ratio .001
      - echo cache-try-flush-evict-all
      - rados -p cache cache-try-flush-evict-all
      - sleep 5
      - echo cache-flush-evict-all
      - rados -p cache cache-flush-evict-all
      - sleep 5
      - echo remove overlay
      - sudo stone osd tier remove-overlay base
      - sleep 20
      # Disabled due to https://tracker.stone.com/issues/46323
      #- echo add writeback overlay
      #- sudo stone osd tier cache-mode cache writeback
      #- sudo stone osd pool set cache cache_target_full_ratio .8
      #- sudo stone osd tier set-overlay base cache
      #- sleep 30
      #- sudo stone osd tier cache-mode cache readproxy
      - done
- rados:
    clients: [client.0]
    pools: [base]
    max_seconds: 600
    ops: 400000
    objects: 10000
    size: 1024
    op_weights:
      read: 100
      write: 100
      delete: 50
      copy_from: 50
