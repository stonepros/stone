tasks:

# stop kernel nfs server, if running
- vip.exec:
    all-hosts:
      - systemctl stop nfs-server

- stoneadm.apply:
    specs:
      - service_type: nfs
        service_id: foo
- stoneadm.wait_for_service:
    service: nfs.foo
