meta:
  - desc: "Containerized prometheus and grafana"
overrides:
  stonemetrics:
    group_vars:
      all:
        prometheus:
          containerized: true
        grafana:
          containerized: true
