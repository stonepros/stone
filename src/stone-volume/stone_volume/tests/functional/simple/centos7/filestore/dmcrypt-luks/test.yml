---

- hosts: osds
  become: yes
  tasks:

    - name: list all OSD directories
      find:
        paths: /var/lib/stone/osd
        file_type: directory
      register: osd_paths

    - name: scan all OSD directories
      command: "stone-volume --cluster={{ cluster }} simple scan {{ item.path }}"
      environment:
        STONE_VOLUME_DEBUG: 1
      with_items:
        - "{{ osd_paths.files }}"

    - name: list all OSD JSON files
      find:
        paths: /etc/stone/osd
        file_type: file
      register: osd_configs

    - name: activate all scanned OSDs
      command: "stone-volume --cluster={{ cluster }} simple activate --file {{ item.path }}"
      environment:
        STONE_VOLUME_DEBUG: 1
      with_items:
        - "{{ osd_configs.files }}"
